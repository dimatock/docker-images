{
  "name": "dev-env",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "dev",
  "containerUser": "dev",
  "updateRemoteUserUID": true,
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/${localWorkspaceFolderBasename},type=bind,consistency=cached",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "shutdownAction": "stopContainer",
  "postCreateCommand": "if [ -f .tool-versions ] || [ -f mise.toml ]; then /usr/local/bin/mise trust . && /usr/local/bin/mise install --yes; fi",
  "postStartCommand": "if [ -S /var/run/docker.sock ]; then DOCKER_GID=$(stat -c '%g' /var/run/docker.sock); if [ \"$DOCKER_GID\" -eq 0 ]; then sudo chmod 666 /var/run/docker.sock; else if getent group docker >/dev/null 2>&1; then sudo groupmod -g \"$DOCKER_GID\" docker; else sudo groupadd -g \"$DOCKER_GID\" docker; fi; sudo usermod -aG docker dev; fi; fi"
}
